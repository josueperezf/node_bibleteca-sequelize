const ESTADOS           = require('./estados.enum');
const ESTATUS_PRESTAMO  = require('./estatus-prestamo.enum');
const TIPO_USUARIO      = require('./tipoUsuario.enum');

module.exports = {
    ESTADOS,
    ESTATUS_PRESTAMO,
    TIPO_USUARIO
}